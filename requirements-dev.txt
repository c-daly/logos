# Development dependencies for LOGOS Foundry
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Code quality
ruff>=0.1.0
mypy>=1.7.0

# Production dependencies are listed in pyproject.toml
# Install the package itself with: pip install -e .
