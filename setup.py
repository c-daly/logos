"""
Setup script for LOGOS Foundry.

This setup.py provides compatibility for pip install while pyproject.toml
remains the canonical source of package metadata.
"""

from setuptools import setup

# Setup configuration is in pyproject.toml
# This file exists for backward compatibility and editable installs
setup()
